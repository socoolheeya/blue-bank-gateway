spring:
  data:
    redis:
      host: localhost
      port: 6379
      password:  # 비밀번호 없음 (로컬 개발용)
      timeout: 2000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0

logging:
  level:
    org.springframework.cloud.gateway: DEBUG
    reactor.netty: INFO
